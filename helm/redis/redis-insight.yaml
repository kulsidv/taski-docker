redis:
  host: redis-master.redis.svc.cluster.local
  port: 6379

ingress:
  enabled: true
  pathType: Prefix
  hosts:
    - host: redis-insight.taski-app.localhost.ru
      paths:
        - /
  tls: []

persistentVolumeClaim:
  create: true